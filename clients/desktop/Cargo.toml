[package]
name = "desktop"
version = "0.1.2"
edition = "2024"

[dependencies]
tauri = { version = "2", features = [] }
web = { path = "../web" }

[build-dependencies]
tauri-build = { version = "2", features = [] }
